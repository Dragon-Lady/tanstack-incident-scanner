# This repository has moved

**`tanstack-incident-scanner` evolved into [`supply-chain-check`](https://github.com/Dragon-Lady/supply-chain-check)** when the tool outgrew its original single-incident scope.

This stub exists intentionally:

- It breaks GitHub's silent rename-redirect, so pushes to the old name **fail loudly** instead of silently landing in the wrong place.
- If your clone, script, or agent hit an error pushing here, update your remote:

```bash
git remote set-url origin https://github.com/Dragon-Lady/supply-chain-check.git
```

This repository is archived and accepts no changes.
